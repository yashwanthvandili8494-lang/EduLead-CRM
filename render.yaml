services:
  - type: web
    name: edulead-api
    runtime: python
    rootDir: server_python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: JWT_SECRET
        generateValue: true
